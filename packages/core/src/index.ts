// packages/core/src/index.ts
//
// Phase 1 placeholder. Phase 2 will move the per-domain "data" and
// "actions" layers from apps/web into here so apps/api and
// apps/workers can reuse business logic. Today the layers still live
// in apps/web/{data,actions} since extracting them is a Phase 2 task,
// not a Phase 1 task.

export const PHASE_1_PLACEHOLDER = true;
